# Postgres
POSTGRES_USER=blogviagem
POSTGRES_PASSWORD=changeme
POSTGRES_DB=blogviagem
DATABASE_URL=postgres://blogviagem:changeme@postgres:5432/blogviagem

# Payload
PAYLOAD_SECRET=changeme-generate-a-real-secret

# Next.js
NEXT_PUBLIC_SERVER_URL=http://localhost:3000

# Template do site institucional (rotas de preview/cron do Payload website template)
CRON_SECRET=changeme-generate-a-real-secret
PREVIEW_SECRET=changeme-generate-a-real-secret

# DECISION: variável auto-populada pela plataforma Vercel em deploys de produção
# (não existe em dev local/Docker Compose). Usada só como fallback opcional em
# src/utilities/getURL.ts e nas rotas de sitemap — NEXT_PUBLIC_SERVER_URL já cobre
# o caso local. Documentada aqui apenas para não faltar nenhuma var lida pelo código.
# VERCEL_PROJECT_PRODUCTION_URL=
